{
  "name": "alterego-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Alter Ego backend: content delivery, evidence database, entitlements and analytics.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "test": "node --test",
    "lint": "node --check src/index.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "fastify": "^5.1.0",
    "google-auth-library": "^9.15.0",
    "pg": "^8.13.1",
    "zod": "^3.23.8"
  }
}
